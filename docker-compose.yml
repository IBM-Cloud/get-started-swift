app:
  image: ibmcom/swift-ubuntu:4.0.3
  ports:
    - "8080:8080"
  volumes:
    - .:/GetStartedSwift
  command: bash -c "cd /GetStartedSwift && swift build && ./.build/x86_64-unknown-linux/debug/get-started-swift"
